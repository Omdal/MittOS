DISKLOCATIONDATALENGTH equ 6

FAT_DIR_NAME equ 0
FAT_DIR_ATTR equ 11
FAT_DIR_NTRES equ 12
FAT_DIR_CREATE_TIME_TENTH equ 13
FAT_DIR_CREATE_TIME equ 14
FAT_DIR_CREATE_DATE equ 16
FAT_DIR_LAST_ACCESS_DATE equ 18
FAT_DIR_FIRST_CLUSTER_HI equ 20
FAT_DIR_LAST_EDIT_TIME equ 22
FAT_DIR_LAST_EDIT_DATE equ 24
FAT_DIR_FIRST_CLUSTER_LOW equ 26
FAT_DIR_FILE_SIZE equ 28
FAT_DIR_DATASIZE equ 32

ATTR_READ_ONLY equ 0x01
ATTR_HIDDEN equ 0x02
ATTR_SYSTEM equ 0x04
ATTR_VOLUME_ID equ 0x08
ATTR_DIRECTORY equ 0x10
ATTR_ARCHIVE equ 0x20


    org A000h
DISKSECTORA:
    
    org A200h
DISKSECTORB:

    org A400h
HDD_DISKA:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKB:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKC:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKD:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKE:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKF:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKG:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKH:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKI:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKJ:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKK:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKL:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKM:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKN:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKO:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKP:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKQ:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKR:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKS:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKT:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKU:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKV:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKW:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKX:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKY:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKZ:
    org $+DISKLOCATIONDATALENGTH

HDD_CURRENT_LOCATION:
    org $+DISKLOCATIONDATALENGTH
HDD_TEMP_LOCATION:
    org $+DISKLOCATIONDATALENGTH
HDD_FOLDER_LOCATION:
    org $+DISKLOCATIONDATALENGTH
HDD_ROOT_FOLDER:
    org $+DISKLOCATIONDATALENGTH
HDD_CURRENT_DRIVE:
    org $+1
HDD_SECTORS_PER_CLUSTER:
    org $+1
HDD_TEMP_CLUSTER:
    org $+4

FAT_FOLDER_DEPTH:
    org $+1
FAT_FOLDERS:
    org $+8*16 ; 16 folders deep?

    org DISKSECTORA+13
FAT_SECTORS_PER_CLUSTER:
    org DISKSECTORA+14
FAT_RESERVED_SECTORS:
    org DISKSECTORA+16
FAT_NUM_FAT:

    org DISKSECTORA+22
FAT16_FAT_SECTOR_SIZE_:

    org DISKSECTORA+36
FAT32_FAT_SECTOR_SIZE_:






HDD_COUNT:
    org A301h
HDD_CURRENT_HDD:
    org A302h
HDD_CURRENT_ID:
    org A303h
HDD_CURRENT_OFFSET:
    org A304h
HDD_FOLDER0:
    org A308h
HDD_FOLDER1:
    org A30Bh
HDD_FOLDER2:
    org A310h
HDD_FOLDER3:
    org A314h
HDD_FOLDER4:
    org A318h
HDD_FOLDER5:
    org A31Bh
HDD_FOLDER6:
    org A320h
HDD_FOLDER7:
    org A324h
HDD_FOLDER8:
    org A328h
HDD_FOLDER9:
    org A32Bh
HDD_FOLDERA:
    org A330h
HDD_FOLDERB:
    org A334h
HDD_FOLDERC:
    org A338h
HDD_FOLDERD:
    org A33Bh
HDD_FOLDERE:
    org A340h
HDD_FOLDERF:
    org A344h
    org A3ABh
    org A3B0h
HDD_TYPE:
    org A3D0h
HDD_PATH: