DISKLOCATIONDATALENGTH equ 6

FAT_DIR_NAME equ 0
FAT_DIR_ATTR equ 11
FAT_DIR_NTRES equ 12
FAT_DIR_CREATE_TIME_TENTH equ 13
FAT_DIR_CREATE_TIME equ 14
FAT_DIR_CREATE_DATE equ 16
FAT_DIR_LAST_ACCESS_DATE equ 18
FAT_DIR_FIRST_CLUSTER_HI equ 20
FAT_DIR_LAST_EDIT_TIME equ 22
FAT_DIR_LAST_EDIT_DATE equ 24
FAT_DIR_FIRST_CLUSTER_LOW equ 26
FAT_DIR_FILE_SIZE equ 28
FAT_DIR_DATASIZE equ 32
FAT_FOLDERNAME_LENGTH equ 8

ATTR_READ_ONLY equ 0x01
ATTR_HIDDEN equ 0x02
ATTR_SYSTEM equ 0x04
ATTR_VOLUME_ID equ 0x08
ATTR_DIRECTORY equ 0x10
ATTR_ARCHIVE equ 0x20


    org A000h
DISKSECTORA:
    
    org A200h
DISKSECTORB:

    org A400h
HDD_DISKA:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKB:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKC:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKD:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKE:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKF:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKG:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKH:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKI:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKJ:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKK:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKL:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKM:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKN:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKO:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKP:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKQ:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKR:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKS:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKT:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKU:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKV:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKW:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKX:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKY:
    org $+DISKLOCATIONDATALENGTH
HDD_DISKZ:
    org $+DISKLOCATIONDATALENGTH

HDD_CURRENT_LOCATION:               ; Disk cursor
    org $+DISKLOCATIONDATALENGTH
HDD_TEMP_LOCATION:                  ; Temp cursor
    org $+DISKLOCATIONDATALENGTH
HDD_FOLDER_LOCATION:                ; Current folder cursor
    org $+DISKLOCATIONDATALENGTH
HDD_ROOT_FOLDER:                    ; Root folder
    org $+DISKLOCATIONDATALENGTH
HDD_CURRENT_DRIVE_INDEX:
    org $+1
HDD_SECTORS_PER_CLUSTER:
    org $+1
HDD_TEMP_CLUSTER:
    org $+4

FAT_FOLDER_DEPTH:
    org $+1
FAT_FOLDERS:
    org $+(FAT_FOLDERNAME_LENGTH*16) ; 16 folders deep?
FAT_TEST_NAME:
    org $+11
FAT_TARGET_NAME:
    org $+11

    org DISKSECTORA+13
FAT_SECTORS_PER_CLUSTER:
    org DISKSECTORA+14
FAT_RESERVED_SECTORS:
    org DISKSECTORA+16
FAT_NUM_FAT:

    org DISKSECTORA+22
FAT16_FAT_SECTOR_SIZE_:

    org DISKSECTORA+36
FAT32_FAT_SECTOR_SIZE_:



    org B000h
PROGRAMSECTORA:
    org $+512
PROGRAMSECTORB:
    org $+512
PROGRAMSECTORC:
    org $+512
PROGRAMSECTORD:
